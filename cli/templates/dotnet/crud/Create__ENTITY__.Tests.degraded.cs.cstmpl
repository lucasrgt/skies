using MyApp.Api;
using MyApp.Api.Modules.__MODULE__;

namespace MyApp.Tests.Modules.__MODULE__;

// __ENTITY__ has complex/VO fields the generator cannot construct, so an end-to-end create is left to
// the owner: Create__ENTITY__.Handle persists the entity, which requires the VO/enum fields the generator
// listed. This degraded test is a placeholder proving the input contract; once you wire the complex
// fields into Create__ENTITY__, replace it with a real round-trip (create, then assert the row exists).
public class Create__ENTITY__Tests
{
    [Unit]
    [Fact(Skip = "__ENTITY__ has complex/VO fields — finish Create__ENTITY__ then write a real round-trip")]
    public Task Creates_an_item_in_the_tenant()
    {
        _ = new Create__ENTITY__.Input(__CREATE_CALL_ARGS__);
        return Task.CompletedTask;
    }
}
