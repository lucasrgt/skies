// Copy for the app frame and the write-side notes every mutation posts. One export per locale, the same keys in each.
export const en = {
  title: "{{ title }}",
  "nav.home": "Home",
  saved: "Saved.",
  failed: "Something went wrong. Try again.",
} as const;
