## Build output
bin/
obj/

## Build-time OpenAPI contract emitted by `dotnet build` (the GetDocument tool). A demo artifact in the sample;
## a consuming app commits its own where the typed client generates from.
examples/sample-app/backend/Sample.Api/contract/
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Bb]in/
[Oo]bj/
# Publishable Node package executables are source, not build output.
!node-sdk/packages/*/bin/
!node-sdk/packages/*/bin/*.mjs

## .NET / IDE
*.user
*.suo
*.userprefs
.vs/
.idea/
*.swp

## Test / coverage
[Tt]est[Rr]esults/
*.trx
*.coverage
coverage/

## Local NuGet feed + packages (build artifacts)
## anchored to root so it ignores the NuGet drop dir, NOT frontend source packages
local-feed/
/packages/
*.nupkg

## Node (template clients, if any)
node_modules/
## Dart and Flutter package-local output
.dart_tool/
flutter-sdk/packages/*/pubspec.lock
flutter-sdk/packages/*/build/
## generator/doctor test scratch (written under cwd; cleaned up after)
frontend/tools/.scaffold-tmp-*/
frontend/tools/.e2e-doctor-tmp-*/

## OS
.DS_Store
Thumbs.db

# vitest/vite dev cache (repo-root runs)
.vite/
# CSM-managed local configuration and disposable projections
.skies/csm/**/config.local.toml
.skies/csm/**/index*.sqlite
.skies/csm/**/index*.sqlite3

# Taskfleet local orchestration state
.taskfleet/

# Rust
/target/
